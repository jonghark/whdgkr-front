class Config {
  // Switch to true to use API backend instead of mock data
  static const bool useApiRepository = false;
  
  // Backend API base URL
  static const String apiBaseUrl = "http://localhost:8080";
}
